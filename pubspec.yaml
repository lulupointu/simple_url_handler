name: simple_url_handler
description: A Flutter package to help you handle the url easily.
version: 0.0.1-nullsafety.1
homepage: https://github.com/lulupointu/simple_url_handler

environment:
  sdk: ">=2.12.0-0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
